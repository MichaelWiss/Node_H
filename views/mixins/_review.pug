mixin review(review)
  .review__header
    .review__author
      img.avatar(src=review.author.gravatar)
      p= review.author.name
    .review__stars(title=`Rated ${review.rating} our 5 stars`)
      = `★`.repeat(review.rating)
    time.review__time
  .review__body
    p= review.text